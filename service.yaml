apiVersion: v1
kind: Service
metadata:
  annotations:
    argocd.argoproj.io/sync-wave: "40"
  name: my-service
  namespace: hns-argo-test-pg
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9376
